import "./paths-B9jPXz5d.js";
import { p as defaultRuntime, r as enableConsoleCapture } from "./subsystem-D2eqGDrA.js";
import "./utils-XeuG5BG2.js";
import "./boolean-DtWR5bt3.js";
import { a as normalizeEnv, d as getPositiveIntFlagValue, f as getPrimaryCommand, h as hasHelpOrVersion, l as getCommandPath, m as hasFlag, o as normalizeWindowsArgv, p as getVerboseFlag, r as isTruthyEnvValue, u as getFlagValue } from "./entry.js";
import { On as loadDotEnv } from "./auth-profiles-CZvcgpJo.js";
import "./agent-scope-a8AT_RWh.js";
import "./openclaw-root-PXe5cw0N.js";
import "./exec-CSag7MO2.js";
import "./github-copilot-token-CHThtPpe.js";
import "./host-env-security-DWcSD4kP.js";
import { t as VERSION } from "./version-DR9Qjj6f.js";
import "./env-vars-tQ4AIdQq.js";
import "./manifest-registry-yJ4VbgqY.js";
import "./zod-schema.sensitive-DWhkVThK.js";
import "./dock-CCwltZZY.js";
import "./model-CJQJEjak.js";
import "./pi-model-discovery-DECvU61g.js";
import "./frontmatter-CyFnddo-.js";
import "./skills-HX9fGcwF.js";
import "./path-alias-guards-Bqfz8RAo.js";
import "./message-channel-yd0kgwA7.js";
import "./sessions-Bi0azzm9.js";
import "./plugins-BzP494ZB.js";
import "./accounts-DPFlmjrY.js";
import "./accounts-CR81YEDz.js";
import "./logging-D-Jq2wIo.js";
import "./accounts-BsTMZ8Rl.js";
import "./bindings-Bdjgy182.js";
import "./send-DtWen-Cr.js";
import "./send-D5ADpplM.js";
import "./subagent-registry-BEhIECVo.js";
import "./paths-BUjNRwj7.js";
import "./chat-envelope-n7RmUTHV.js";
import "./client-CkwAK9X6.js";
import "./call-sYT50rpM.js";
import "./pairing-token-BksY3ZD6.js";
import "./net-BmOGX3t_.js";
import "./tailnet-BEMQ0WkF.js";
import "./tokens-TehTRAdt.js";
import "./with-timeout-Dzh9Mm9M.js";
import "./deliver-BGjVHnsp.js";
import "./diagnostic-CYZgmzrv.js";
import "./diagnostic-session-state-C8NH3PSI.js";
import "./send-CkynSxxD.js";
import "./image-ops-C1wqTSdk.js";
import "./pi-embedded-helpers-1PZ4aCg-.js";
import "./sandbox-DTBpCi6a.js";
import "./tool-catalog-DfXt8uIy.js";
import "./chrome-CLydEGCA.js";
import "./tailscale-BVHEqtVC.js";
import "./auth-B4wlyb-l.js";
import "./server-context-CgtJLU5F.js";
import "./paths-BeiNGh4n.js";
import "./redact-BGmCI-cn.js";
import { r as formatUncaughtError } from "./errors-BFjy26zi.js";
import "./fs-safe-BEKG0MwK.js";
import "./ssrf-gi8Pc2Fy.js";
import "./store-Do_Iey7m.js";
import "./ports-BwOg8fHr.js";
import "./trash-Cv3xOBdi.js";
import "./server-middleware-dIszEUpi.js";
import "./tool-images-CRkRwEVE.js";
import "./thinking-DuXsFbTV.js";
import "./models-config-pDwBB9Bk.js";
import "./exec-approvals-allowlist-B6arSB0T.js";
import "./exec-safe-bin-runtime-policy-BwT46TA_.js";
import "./reply-prefix-B0pLVTKg.js";
import "./memory-cli-Dz7xGF6Z.js";
import "./manager-DJ2nuij3.js";
import "./gemini-auth-BwpwqMEH.js";
import "./fetch-guard-DdlITp72.js";
import "./query-expansion-C6x4pKyg.js";
import "./retry-BoP38Evl.js";
import "./target-errors-zRZr7S-r.js";
import "./local-roots-DC5tcBeV.js";
import "./chunk-CDx-ekK4.js";
import "./markdown-tables-DCHQRiwX.js";
import "./ir-BufFMQ3o.js";
import "./render-e7fENCYH.js";
import "./commands-KKgWclzv.js";
import "./commands-registry-Dg8Rixk-.js";
import "./image-UdZytPX6.js";
import "./tool-display-TYTccNWv.js";
import "./retry-policy-CtbxW3Xf.js";
import { u as installUnhandledRejectionHandler } from "./runner-QvrPoSIs.js";
import "./model-catalog-B2D_BloA.js";
import "./fetch-C-_zRqDA.js";
import "./pairing-store-DjFn_dIY.js";
import "./exec-approvals-BMFhaWeS.js";
import "./nodes-screen-BQT-wV6c.js";
import "./system-run-command-DlF20rUR.js";
import "./session-utils-HX7tx2AA.js";
import "./session-cost-usage-rB-A7vpt.js";
import "./skill-commands-CVfpjHAz.js";
import "./workspace-dirs-BJKGMQ48.js";
import "./tables-D_QG3PE3.js";
import "./server-lifecycle-BKrYFSKV.js";
import "./stagger-DjNEVTNM.js";
import "./channel-selection-BVseVG8h.js";
import "./plugin-auto-enable-BZsoToeO.js";
import "./send-CUyX396a.js";
import "./outbound-attachment-Bzghzktq.js";
import "./delivery-queue-sFkGs-VC.js";
import "./send-B34oK_cg.js";
import "./resolve-route-Bv0lzNr2.js";
import "./pi-tools.policy-DobOP4bl.js";
import "./proxy-DZJY4nKm.js";
import "./links-CWyy563z.js";
import "./cli-utils-B7nApBgk.js";
import "./help-format-DYI1Qsgl.js";
import "./progress-CoLN8zGu.js";
import "./replies-BPCok6xi.js";
import "./onboard-helpers-gyti7QOy.js";
import "./prompt-style-BcyiLcfo.js";
import "./pairing-labels-D55D0emZ.js";
import { t as ensureOpenClawCliOnPath } from "./path-env-s9nOTFhf.js";
import "./note-Bpomg9Vl.js";
import { t as ensurePluginRegistryLoaded } from "./plugin-registry-CsQC94Gr.js";
import { t as assertSupportedRuntime } from "./runtime-guard-JlKhn9Xx.js";
import { t as emitCliBanner } from "./banner-CSJU9kzh.js";
import "./doctor-config-flow-BPFVDJ5o.js";
import { n as ensureConfigReady } from "./config-guard-BAaVO_XW.js";
import process$1 from "node:process";
import { fileURLToPath } from "node:url";

//#region src/cli/program/routes.ts
const routeHealth = {
	match: (path) => path[0] === "health",
	loadPlugins: (argv) => !hasFlag(argv, "--json"),
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const verbose = getVerboseFlag(argv, { includeDebug: true });
		const timeoutMs = getPositiveIntFlagValue(argv, "--timeout");
		if (timeoutMs === null) return false;
		const { healthCommand } = await import("./health-DucjJfYw.js").then((n) => n.i);
		await healthCommand({
			json,
			timeoutMs,
			verbose
		}, defaultRuntime);
		return true;
	}
};
const routeStatus = {
	match: (path) => path[0] === "status",
	loadPlugins: true,
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const deep = hasFlag(argv, "--deep");
		const all = hasFlag(argv, "--all");
		const usage = hasFlag(argv, "--usage");
		const verbose = getVerboseFlag(argv, { includeDebug: true });
		const timeoutMs = getPositiveIntFlagValue(argv, "--timeout");
		if (timeoutMs === null) return false;
		const { statusCommand } = await import("./status-D1Ixgwvu.js").then((n) => n.t);
		await statusCommand({
			json,
			deep,
			all,
			usage,
			timeoutMs,
			verbose
		}, defaultRuntime);
		return true;
	}
};
const routeSessions = {
	match: (path) => path[0] === "sessions" && !path[1],
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const allAgents = hasFlag(argv, "--all-agents");
		const agent = getFlagValue(argv, "--agent");
		if (agent === null) return false;
		const store = getFlagValue(argv, "--store");
		if (store === null) return false;
		const active = getFlagValue(argv, "--active");
		if (active === null) return false;
		const { sessionsCommand } = await import("./sessions-C8N7jD6f.js").then((n) => n.n);
		await sessionsCommand({
			json,
			store,
			agent,
			allAgents,
			active
		}, defaultRuntime);
		return true;
	}
};
const routeAgentsList = {
	match: (path) => path[0] === "agents" && path[1] === "list",
	run: async (argv) => {
		const json = hasFlag(argv, "--json");
		const bindings = hasFlag(argv, "--bindings");
		const { agentsListCommand } = await import("./agents-DRxS1XRQ.js").then((n) => n.t);
		await agentsListCommand({
			json,
			bindings
		}, defaultRuntime);
		return true;
	}
};
const routeMemoryStatus = {
	match: (path) => path[0] === "memory" && path[1] === "status",
	run: async (argv) => {
		const agent = getFlagValue(argv, "--agent");
		if (agent === null) return false;
		const json = hasFlag(argv, "--json");
		const deep = hasFlag(argv, "--deep");
		const index = hasFlag(argv, "--index");
		const verbose = hasFlag(argv, "--verbose");
		const { runMemoryStatus } = await import("./memory-cli-Dz7xGF6Z.js").then((n) => n.t);
		await runMemoryStatus({
			agent,
			json,
			deep,
			index,
			verbose
		});
		return true;
	}
};
function getCommandPositionals(argv) {
	const out = [];
	const args = argv.slice(2);
	for (const arg of args) {
		if (!arg || arg === "--") break;
		if (arg.startsWith("-")) continue;
		out.push(arg);
	}
	return out;
}
function getFlagValues(argv, name) {
	const values = [];
	const args = argv.slice(2);
	for (let i = 0; i < args.length; i += 1) {
		const arg = args[i];
		if (!arg || arg === "--") break;
		if (arg === name) {
			const next = args[i + 1];
			if (!next || next === "--" || next.startsWith("-")) return null;
			values.push(next);
			i += 1;
			continue;
		}
		if (arg.startsWith(`${name}=`)) {
			const value = arg.slice(name.length + 1).trim();
			if (!value) return null;
			values.push(value);
		}
	}
	return values;
}
const routes = [
	routeHealth,
	routeStatus,
	routeSessions,
	routeAgentsList,
	routeMemoryStatus,
	{
		match: (path) => path[0] === "config" && path[1] === "get",
		run: async (argv) => {
			const pathArg = getCommandPositionals(argv)[2];
			if (!pathArg) return false;
			const json = hasFlag(argv, "--json");
			const { runConfigGet } = await import("./config-cli-BywjEXoA.js");
			await runConfigGet({
				path: pathArg,
				json
			});
			return true;
		}
	},
	{
		match: (path) => path[0] === "config" && path[1] === "unset",
		run: async (argv) => {
			const pathArg = getCommandPositionals(argv)[2];
			if (!pathArg) return false;
			const { runConfigUnset } = await import("./config-cli-BywjEXoA.js");
			await runConfigUnset({ path: pathArg });
			return true;
		}
	},
	{
		match: (path) => path[0] === "models" && path[1] === "list",
		run: async (argv) => {
			const provider = getFlagValue(argv, "--provider");
			if (provider === null) return false;
			const all = hasFlag(argv, "--all");
			const local = hasFlag(argv, "--local");
			const json = hasFlag(argv, "--json");
			const plain = hasFlag(argv, "--plain");
			const { modelsListCommand } = await import("./models-Cwg7n-KB.js").then((n) => n.t);
			await modelsListCommand({
				all,
				local,
				provider,
				json,
				plain
			}, defaultRuntime);
			return true;
		}
	},
	{
		match: (path) => path[0] === "models" && path[1] === "status",
		run: async (argv) => {
			const probeProvider = getFlagValue(argv, "--probe-provider");
			if (probeProvider === null) return false;
			const probeTimeout = getFlagValue(argv, "--probe-timeout");
			if (probeTimeout === null) return false;
			const probeConcurrency = getFlagValue(argv, "--probe-concurrency");
			if (probeConcurrency === null) return false;
			const probeMaxTokens = getFlagValue(argv, "--probe-max-tokens");
			if (probeMaxTokens === null) return false;
			const agent = getFlagValue(argv, "--agent");
			if (agent === null) return false;
			const probeProfileValues = getFlagValues(argv, "--probe-profile");
			if (probeProfileValues === null) return false;
			const probeProfile = probeProfileValues.length === 0 ? void 0 : probeProfileValues.length === 1 ? probeProfileValues[0] : probeProfileValues;
			const json = hasFlag(argv, "--json");
			const plain = hasFlag(argv, "--plain");
			const check = hasFlag(argv, "--check");
			const probe = hasFlag(argv, "--probe");
			const { modelsStatusCommand } = await import("./models-Cwg7n-KB.js").then((n) => n.t);
			await modelsStatusCommand({
				json,
				plain,
				check,
				probe,
				probeProvider,
				probeProfile,
				probeTimeout,
				probeConcurrency,
				probeMaxTokens,
				agent
			}, defaultRuntime);
			return true;
		}
	}
];
function findRoutedCommand(path) {
	for (const route of routes) if (route.match(path)) return route;
	return null;
}

//#endregion
//#region src/cli/route.ts
async function prepareRoutedCommand(params) {
	const suppressDoctorStdout = hasFlag(params.argv, "--json");
	emitCliBanner(VERSION, { argv: params.argv });
	await ensureConfigReady({
		runtime: defaultRuntime,
		commandPath: params.commandPath,
		...suppressDoctorStdout ? { suppressDoctorStdout: true } : {}
	});
	if (typeof params.loadPlugins === "function" ? params.loadPlugins(params.argv) : params.loadPlugins) ensurePluginRegistryLoaded();
}
async function tryRouteCli(argv) {
	if (isTruthyEnvValue(process.env.OPENCLAW_DISABLE_ROUTE_FIRST)) return false;
	if (hasHelpOrVersion(argv)) return false;
	const path = getCommandPath(argv, 2);
	if (!path[0]) return false;
	const route = findRoutedCommand(path);
	if (!route) return false;
	await prepareRoutedCommand({
		argv,
		commandPath: path,
		loadPlugins: route.loadPlugins
	});
	return route.run(argv);
}

//#endregion
//#region src/cli/run-main.ts
function rewriteUpdateFlagArgv(argv) {
	const index = argv.indexOf("--update");
	if (index === -1) return argv;
	const next = [...argv];
	next.splice(index, 1, "update");
	return next;
}
function shouldSkipPluginCommandRegistration(params) {
	if (params.hasBuiltinPrimary) return true;
	if (!params.primary) return hasHelpOrVersion(params.argv);
	return false;
}
function shouldEnsureCliPath(argv) {
	if (hasHelpOrVersion(argv)) return false;
	const [primary, secondary] = getCommandPath(argv, 2);
	if (!primary) return true;
	if (primary === "status" || primary === "health" || primary === "sessions") return false;
	if (primary === "config" && (secondary === "get" || secondary === "unset")) return false;
	if (primary === "models" && (secondary === "list" || secondary === "status")) return false;
	return true;
}
async function runCli(argv = process$1.argv) {
	const normalizedArgv = normalizeWindowsArgv(argv);
	loadDotEnv({ quiet: true });
	normalizeEnv();
	if (shouldEnsureCliPath(normalizedArgv)) ensureOpenClawCliOnPath();
	assertSupportedRuntime();
	if (await tryRouteCli(normalizedArgv)) return;
	enableConsoleCapture();
	const { buildProgram } = await import("./program-BnScETrD.js");
	const program = buildProgram();
	installUnhandledRejectionHandler();
	process$1.on("uncaughtException", (error) => {
		console.error("[openclaw] Uncaught exception:", formatUncaughtError(error));
		process$1.exit(1);
	});
	const parseArgv = rewriteUpdateFlagArgv(normalizedArgv);
	const primary = getPrimaryCommand(parseArgv);
	if (primary) {
		const { getProgramContext } = await import("./program-context-D-_wfauA.js").then((n) => n.n);
		const ctx = getProgramContext(program);
		if (ctx) {
			const { registerCoreCliByName } = await import("./command-registry-CjugGHzS.js").then((n) => n.t);
			await registerCoreCliByName(program, ctx, primary, parseArgv);
		}
		const { registerSubCliByName } = await import("./register.subclis-DbX_s5ba.js").then((n) => n.a);
		await registerSubCliByName(program, primary);
	}
	if (!shouldSkipPluginCommandRegistration({
		argv: parseArgv,
		primary,
		hasBuiltinPrimary: primary !== null && program.commands.some((command) => command.name() === primary)
	})) {
		const { registerPluginCliCommands } = await import("./cli-BTApOFIw.js");
		const { loadConfig } = await import("./auth-profiles-CZvcgpJo.js").then((n) => n.D);
		registerPluginCliCommands(program, loadConfig());
	}
	await program.parseAsync(parseArgv);
}

//#endregion
export { runCli };